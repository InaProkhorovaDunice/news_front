import axios from 'axios';
import { apiUrl } from '../config';
import { getLocalStorageItem } from './useLocalStorage';
import queryString from 'query-string';

export const safeAxios = (url, options) => {
};
